
# Secrets
.env
.env.*
!.env.example

# Submission artifacts and transcripts (uploaded separately to HackerRank)
code.zip
September_Final_Audit_Evidence.zip
log.txt
chat_transcript.txt
scratch/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
venv_freeze_tmp.txt

# Local LLM harness cache (recreatable from dataset/ inputs; never shipped as-is)
.llm_cache/

# Local scratch / caches (not part of the submission)
.cache/
.empty_test_cache/
*.tmp
